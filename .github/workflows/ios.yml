name: iOS CI (Build & Test)

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-and-test:
    runs-on: macos-14

    steps:
      - name: Set up Xcode 16.2
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.2'

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Resolve SwiftPM dependencies
        run: |
          xcodebuild -resolvePackageDependencies -project TheRoamingKin.xcodeproj

      - name: Run SwiftLint
        run: |
          swift package plugin run swiftlint --target TheRoamingKin

      - name: Build project for testing
        run: |
          xcodebuild build-for-testing \
            -project TheRoamingKin.xcodeproj \
            -scheme TheRoamingKin \
            -destination 'platform=iOS Simulator,name=iPad (10th generation),OS=17.4'

      - name: Run tests
        run: |
          xcodebuild test-without-building \
            -project TheRoamingKin.xcodeproj \
            -scheme TheRoamingKin \
            -destination 'platform=iOS Simulator,name=iPad (10th generation),OS=17.4'
